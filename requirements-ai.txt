# Optional heavy AI engines for Image Studio (PyTorch) — local weights.
#   pip install -r requirements-ai.txt
#   pip install "git+https://github.com/facebookresearch/sam2.git"
#   python scripts/setup_ai_models.py
#
# Runtime inference is local-only from models/.
#
# Note: basicsr/realesrgan often fail on Python ≥3.12 — Spandrel is the
# preferred Real-ESRGAN weight loader (still uses xinntao .pth checkpoints).

torch>=2.5.1
torchvision>=0.20.1

# One-time Hub fetches in setup_ai_models.py (stored under models/)
huggingface_hub>=0.26

# Real-ESRGAN weights loader (xinntao checkpoints under models/realesrgan/)
spandrel>=0.4.0

# Grounding DINO Swin-B uses Transformers for its local BERT text encoder.
transformers>=4.45

# Optional classic stack (may fail on newer Python):
# realesrgan>=0.3.0
# basicsr>=1.4.2
# facexlib>=0.3.0

# SAM 2 — install separately (CUDA extension optional):
#   pip install "git+https://github.com/facebookresearch/sam2.git"

# Soft matte (BiRefNet / RMBG / isnet sessions)
rembg>=2.0.50

# LaMa erase inpainting uses local models/lama/big-lama.pt + torch
# (same torchscript as lama-cleaner / simple-lama-inpainting).
#   python scripts/setup_ai_models.py   # downloads big-lama.pt
# No separate pip package required — runner is in image_studio.ai.lama_runner.
