# GIF Studio — optional environment

# API
GIF_STUDIO_API_HOST=127.0.0.1
GIF_STUDIO_API_PORT=8000
GIF_STUDIO_CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Job queue / memory guard (AI, upscale, interpolate, export)
# GIF_STUDIO_AI_MAX_CONCURRENT=1
# GIF_STUDIO_AI_QUEUE_WAIT_S=120
# GIF_STUDIO_MIN_FREE_RAM_GIB=3.0
# GIF_STUDIO_MIN_FREE_VRAM_GIB=0.35
# GIF_STUDIO_UNLOAD_MODELS=1
# GIF_STUDIO_RAM_RESERVE_UPSCALE_GIB=2.0
# GIF_STUDIO_RAM_RESERVE_EXPORT_GIB=1.5
# Set GIF_STUDIO_UNLOAD_MODELS=0 to keep models warm (faster repeats, more RAM)

# Vite → API proxy
VITE_API_PROXY=http://127.0.0.1:8000

# Persistence / jobs / storage (optional)
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/gif_studio
# REDIS_URL=redis://localhost:6379/0
# S3_BUCKET=gif-studio
# S3_ENDPOINT=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_REGION=us-east-1
# GIF_STUDIO_LOCAL_STORAGE=.gif-studio-storage

# AI models — runners live in src/gif_studio/ai/
# Install: pip install -r requirements-ai.txt && python scripts/setup_ai_models.py
# GIF_STUDIO_MODELS_DIR=models
# GIF_STUDIO_THIRD_PARTY=third_party
# GIF_STUDIO_TORCH_DEVICE=cuda   # or cpu / mps

# SAM 2 (facebookresearch/sam2) — HF auto-download when sam2 package installed
# SAM2_HF_ID=facebook/sam2.1-hiera-tiny
# SAM2_CHECKPOINT=models/sam2/sam2.1_hiera_tiny.pt
# SAM2_CONFIG=configs/sam2.1/sam2.1_hiera_t.yaml

# Grounding DINO (IDEA-Research) — Transformers HF preferred
# GROUNDING_DINO_HF_ID=IDEA-Research/grounding-dino-tiny
# GROUNDING_DINO_CONFIG=...
# GROUNDING_DINO_CHECKPOINT=models/groundingdino/groundingdino_swint_ogc.pth

# Real-ESRGAN (xinntao) — auto-downloads weights on first use
# REALESRGAN_MODEL=models/realesrgan/RealESRGAN_x4plus.pth
# REALESRGAN_TILE=0

# RIFE (hzwer/ECCV2022-RIFE / Practical-RIFE)
# RIFE_REPO=third_party/Practical-RIFE
# RIFE_MODEL=models/rife/train_log

# YOLO_MODEL=yolov8n.pt
# GIF_STUDIO_AI_MODEL=isnet-general-use

# Browser CDN / ONNX overrides (optional)
# VITE_MEDIAPIPE_WASM=https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/wasm
# VITE_MEDIAPIPE_MODEL=https://storage.googleapis.com/mediapipe-models/image_segmenter/selfie_segmenter/float16/latest/selfie_segmenter.tflite
# VITE_MEDIAPIPE_POSE_MODEL=https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/1/pose_landmarker_lite.task
# VITE_FFMPEG_CORE_BASE=https://cdn.jsdelivr.net/npm/@ffmpeg/core@0.12.10/dist/esm
# VITE_SAM2_ENCODER=
# VITE_SAM2_DECODER=
# VITE_GROUNDING_DINO_ONNX=
# VITE_REALESRGAN_ONNX=
# VITE_RIFE_ONNX=
